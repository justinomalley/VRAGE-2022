using UnityEngine;

public class TutorialTrigger : MonoBehaviour
{
    
    // Start is called before the first frame update
    void Start() {
        var input = GameObject.Find("App").GetComponent<RawXRInput>();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
